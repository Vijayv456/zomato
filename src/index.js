import React from 'react';
import ReactDOM from 'react-dom/client';
import Login from './Components.js/ZomatoLogin';
// import DemoCarousel from './Components.js/Carousol';
import Val from './new';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Login />
<Val />
  </React.StrictMode>
);
